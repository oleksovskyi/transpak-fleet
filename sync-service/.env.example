DATABASE_URL="postgresql://user:password@localhost:5432/transpak_fleet"
# Це все — sync-service тепер один процес для всіх клієнтів платформи. Wialon-токен,
# депо, resource id звітів/водіїв кожної компанії живуть у таблиці CompanyWialonConfig
# (БД), не в .env. Керування — через backend:
#   PUT /api/platform/companies/:id/wialon-config  (X-Platform-Key)
# Новий клієнт з телеметрією = новий рядок там, без нового деплою sync-service.
